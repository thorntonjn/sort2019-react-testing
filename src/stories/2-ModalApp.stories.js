import React from 'react';
import ModelApp, { ModalApp } from '../components/modelApp/ModelApp';

export const modalApp = () => (
  <ModelApp >
  </ModelApp>
);

modalApp.story = {
  name: 'to Storybook2',
};

